const Llist = require('./Llist');

class StackByLlst {
  constructor () {
    this.top = 0;
    this.llist = new Llist();
  }

  push(item) {
    if(llist.isEmpty()) {
      llist.insert(item, null);
    }
    // TODO: 寻找最后一个元素
    llist.insert(item, );
  }
}